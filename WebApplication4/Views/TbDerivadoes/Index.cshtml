@model IEnumerable<WebApplication4.Models.TbDerivado>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IdTicketNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdUsuarioNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdTicketNavigation.IdTicket)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdUsuarioNavigation.IdUsuario)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdDerivado">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdDerivado">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdDerivado">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
