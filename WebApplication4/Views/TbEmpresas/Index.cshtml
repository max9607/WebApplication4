@model IEnumerable<WebApplication4.Models.TbEmpresa>

@{
    ViewData["Title"] = "Index";
}

<div class="text-center">
    <h1>Listado de Empresas</h1>
    <hr />
</div>
<p>
    <a type="button" class="btn btn-primary" asp-action="Create">
        <i class="icofont-plus text-light"></i>
        Crear Nuevo</a>
</p>
<table id="Secundaria" class="table align-middle ">
    <thead class="table-dark">
        <tr>
            <th>
                Nombre de la Empresa
            </th>
            <th>
                NIT
            </th>
            <th>
                Teléfono
            </th>
            <th>
                Opciones
            </th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nombre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Nit)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Telefono)
            </td>
                <td class="text-center">
                    <div class="btn-group btn-group-sm" role="group" aria-label="Small button group">
                        <a asp-action="Edit" asp-route-id="@item.IdEmpresa" class="btn btn-outline-dark">Editar</a>
                        <a asp-action="Details" asp-route-id="@item.IdEmpresa" class="btn btn-outline-dark">Detalles</a>
                        
                    </div>
                </td>
        </tr>
}
    </tbody>
</table>
<p>
    <a type="button" class="btn btn-primary" asp-action="Index2">
        <i class="icofont-plus text-light"></i>
        Deshabilitados
    </a>
</p>