@model IEnumerable<WebApplication4.Models.TbFechaTicket>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FechaCreado)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FechaCerrado)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FechaCreado)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FechaCerrado)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdFecha">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdFecha">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdFecha">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
