@model IEnumerable<WebApplication4.Models.TbCategorium>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Problema)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Problema)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdProblema">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdProblema">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdProblema">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
